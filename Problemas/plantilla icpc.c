#include <bits/stdc++.h>
using namespace std;

// --- Definiciones Rápidas ---
#define ll long long
#define all(x) (x).begin(), (x).end()
#define pb push_back
#define fi first
#define se second
#define endl '\n'

// --- Constantes (activar si se usan) ---
// const int MOD = 1e9+7;
// const ll INF = 1e18;

// --- Función Principal del Problema ---
void solve() {
    // Tu código va aquí
    
}

// --- Main ---
int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    
    int t = 1;
    // cin >> t; // Descomenta para múltiples casos de prueba
    while (t--) {
        solve();
    }
    return 0;
}