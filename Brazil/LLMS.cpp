#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    long long N, M, p, x, y, Q, k;
    unordered_map <string, pair<long long, long long>> WordVector;
    cin >> N;

    while(N>0){
        cin >> p >> x >> y;
        WordVector[p] = 
    }
    
    return 0;
}